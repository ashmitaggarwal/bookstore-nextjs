export interface BookType{
    category: string;
    name: string;
    price: number;
    description: string;
}

export interface StoredBookType {
    id: string;
    category: string;
    name: string;
    price: number;
    description: string;
}
